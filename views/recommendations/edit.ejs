<%- include("../partials/header") %>

<div class="container text-center">
    <div class="navigationBar">
        <p><span id="pastSpan" class="activeTense"><a href="/past">PAST</a></span><span id="presentSpan" class="activeTense"><a href="/">PRESENT</a></span><span id="futureSpan" class="activeTense"><a href="/future">FUTURE</a></span></p>
    </div>
    <div class="clock">
        <p><%= today %> | <span id="presentClock">88:88:88</span></p>
    </div>
</div>

<div class="box">
    <div class="column left"></div>
    <div id="middleColumn" class="column middle text-center">
        <iframe id="presentPlayer" width="640" height="360"
        src="https://www.youtube.com/embed/<%= recommendation.youtubeID %>?start=0&autoplay=1&enablejsapi=1&mute=1&controls=0"
        style="border: solid 4px #37474F"
        allow="autoplay"
        ></iframe>
        <div class="row">
            <div class="col-7">
                <div class="userinformation">
                    <p><strong>User: </strong><a href="/users/<%=user._id%>"><%= user.username %></a></p>
                    <p><strong>Country of Identification: </strong><%=user.country%></p>
                    <p><strong>Date of Recommendation: </strong> <%= recommendation.daySKU %></p>
                    <p><strong>Name: </strong><%= recommendation.name %></p>
                    <p><strong>Why did you recommend it?: </strong></p>
                    <div>
                        <p id="recommendationDescription"><%= recommendation.description%></p>
                    </div>
                </div>
            </div>
            <div class="col-5">
                <form action="/recommendations/<%= recommendation._id %>?_method=PUT" method="POST">
                    <div class="form-group">
                        <p>Youtube URL of your recommendation:</p>
                        <p><%= recommendation.url %></p>
                    </div>
    
                    <div class="form-group">
                        <label for="descriptionTextArea" class="clock">Why did you recommend it?</label>
                        <textarea class="form-control" rows="10" cols="33" id="descriptionTextArea" name="description" required><%= recommendation.description %></textarea>
                    </div>
    
                    <label for="userCheckbox" class="userInformation content">Was this created by you?</label>    
                    <input type="checkbox" id="userCheckbox" name="wasCreatedByUser" value="true">
                    <br>
    
                    <div class="form-group">
                        <button class="btn btn-success">Submit!</button>
                        <a class="btn btn-primary" href="/users/<%=user.username%>/recommendations"> Go Back</a>
                    </div>  
                </form>   
            </div>
        </div>
    </div>
    <div class="column right"></div>
</div>

<%- include("../partials/footer") %>
